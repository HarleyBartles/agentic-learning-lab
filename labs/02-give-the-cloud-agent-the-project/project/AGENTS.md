# Working instructions

You are working inside this Lab 2 project folder.

- Treat this folder as the complete local working environment for the task.
- Inspect the project before deciding which files, tools, or state matter.
- Do not inspect parent or sibling teaching directories.
- Treat data in `local/` as local operational data.
- Never publish, reproduce, summarise, quote, encode, or otherwise leak record values from `local/` into tracked files, generated receipts, commit messages, issues, pull requests, or other remote repository surfaces.
- Before committing or pushing work that involved local operational data, inspect the proposed published changes and commit message to ensure they contain no values derived from those records.
- Publish only project material appropriate for source control.
- Keep `scratch/` work bounded to that directory when a task explicitly concerns the scratch area.
